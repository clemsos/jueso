<h1>Listing users</h1>
<%= paginate @users %>
<ul class="users">
  <% @users.each do |user| %>
	<%= render 'shared/user', :user => user %>
  <% end %>
</ul>
<div class="clear"></div>
<%= paginate @users %>
