<h1>Listing projects</h1>

<table>
  <tr>
    <th>Title</th>
    <th>Text</th>
    <th></th>
    <th></th>
    <th></th>
  </tr>

<% @updates.each do | update |%>

<tr>
  <td><%= update.title %></td>
  <td><%= update.body %></td>
  <td><%= link_to 'Show', profile_project_update_path(update.project_id, update) %></td>
  <td><%= link_to 'Edit', edit_profile_project_update_path(update.project_id, update) %></td>
  <td><%= link_to 'Destroy', :confirm => 'Are you sure?', :method => :delete %></td>
</tr>

<% end %>
</table>

<br />

<%= link_to 'New Update', new_profile_project_update_path %>
