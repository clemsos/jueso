<% content_for :scripts do%>

	<%= javascript_include_tag 'jquery-ui-1.8.14.custom.min' %>
	
	<script type="text/javascript" charset="utf-8">	
	
		$( "div.slider" ).slider({
					value:10,
					min: 1,
					max: 20000,
					step: 1,
					slide: function( event, ui ) {
						$( "#amount" ).val(ui.value );
					}
				});
		$( "#amount" ).val($( "div.slider" ).slider( "value") );
			
	
	</script>
<% end %>
	
<%= form_for([@project, @project.backs.build]) do |f| %>

<div class="field">
	<div class="slider"></div>
    <%= f.label :amount %><br />
    <p>¥<%= f.text_field :amount, :id =>"amount", :style => "border:none" %></p>
  </div>

  <div class="field">
    <%= f.label :comment %><br />
    <%= f.text_area :comment, :rows => 5 %>
  </div>

 <div class="actions">
    <%= f.submit %>
  </div>	
<% end %>
