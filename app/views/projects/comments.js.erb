<% if user_signed_in? %> 
	$("#comments .form").html("<%= escape_javascript(render('projects/comment_form')) %>");
<% end %>

$("#comments .list").html("<%= escape_javascript(render('projects/comments')) %>");