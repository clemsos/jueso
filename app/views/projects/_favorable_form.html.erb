<% unless current_user?(@user) %> 
<div id="favorite_form"> 
	<% if current_user.favorable?(@project) %>
		<%= render 'unfavorable' %> 
	<% else %>
		<%= render 'favorable' %> 
	<% end %> 
</div>
<% end %>